# pnpm will warn about project-level auth with env vars.
# In CI, NODE_AUTH_TOKEN is set by GitHub Actions + setup-node with registry-url.
# Locally, use `npm login` or set the token in ~/.npmrc instead.
# This file exists so pnpm knows to use the public registry for @mail-agent/* packages.
registry=https://registry.npmjs.org/
